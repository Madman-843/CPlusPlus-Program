//Header file to declare functions so the clock can have different functions, and look less cluttered.

int AddTime(int standard, int military, int sharedM, int sharedS, int selection);
int Update(int standard, int military, int minutes, int seconds);
int Display(int standard, int military, int minutes, int seconds);
void Menu();
